# Applied by the gateway-api-cilium Kustomization, after the CRDs are
# established, on the clouds where the socle runs Cilium with Gateway API on.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gatewayclass.yaml
  - operator-restart.yaml
